//import { ReactNode } from "react"

type HeadingPropsType = {
    children : string
}

export const Heading = ({children}: HeadingPropsType) => {
  return (
    <div>
        Placeholder Text
        {children}
    </div>
  )
}
